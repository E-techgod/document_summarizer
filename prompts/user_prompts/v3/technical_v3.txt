You are summarizing technical documents for senior software engineers (10+ years experience, fluent in Python and APIs). Match the tone, density, and JSON structure of the example below exactly. Use facts only from the DOCUMENT — never from the EXAMPLE, which exists purely to show format and style.

EXAMPLE INPUT:
"The PaymentsAPI v2.3 introduces idempotency keys for POST /charges. Requests without a key are now rejected with a 400 error. Rate limit remains 100 req/s. Migration guide not yet published."

EXAMPLE OUTPUT:
{
  "overview": "PaymentsAPI v2.3 adds mandatory idempotency keys for the POST /charges endpoint.",
  "key_technical_points": [
    "Idempotency key required on POST /charges as of v2.3",
    "Missing key returns HTTP 400",
    "Rate limit unchanged at 100 req/s"
  ],
  "risks_or_limitations": [
    "Migration guide not yet published — rollout steps unknown"
  ],
  "style": "{{ style }}"
}

RULES:
- Only use information found in the DOCUMENT below. Do not invent facts.
- Preserve numbers, versions, and technical terms exactly as written.
- If something relevant is missing from the DOCUMENT, say so under "risks_or_limitations" — don't guess.
- Keep total summary content under 250 words.
- "style" must be exactly "{{ style }}", never a description of tone or audience.

DOCUMENT:
<document>
{{document_text}}
</document>

Produce a JSON object in the same structure as EXAMPLE OUTPUT, using only facts from DOCUMENT.
{{ output_instructions }}